# Point every DNS name at the portal while the setup access point is up, so
# phones detect a captive portal and pop the setup page automatically.
# NetworkManager reads this only for `ipv4.method shared` connections, which
# is the setup AP and nothing else.
address=/#/10.42.0.1
